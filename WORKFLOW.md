# 🎯 Флоу работы ВидеоМиг

## Полный цикл от производства до получателя

### 1️⃣ Производство (Backend)

**Генерация партии карточек:**

```bash
# Админ открывает админ-панель
http://localhost:3001/admin

# Генерирует партию, например:
- Количество: 100 штук
- Название партии: "Новый год 2026"
```

**Что происходит:**
- Бэкенд генерирует 100 уникальных ID (например: `A1B2C3D4`)
- Для каждой создается ссылка: `https://videomig.ru/c/A1B2C3D4`
- Статус всех карточек: `new` (новая, не отсканирована)
- Сохраняется в базу данных

**Результат:**
- CSV файл со всеми ссылками для программирования NFC
- Формат: `ID, URL, Status, Batch, Created`

---

### 2️⃣ Производство (Физическое)

**Программирование NFC-меток:**

1. Берем NFC-метку (NTAG213/215)
2. Записываем URL: `https://videomig.ru/c/A1B2C3D4`
3. Клеим метку на открытку (внутри или сзади)
4. Печатаем QR-код с той же ссылкой на обороте

**Результат:**
- Физическая открытка с NFC + QR
- Метка содержит уникальную ссылку
- Карточка готова к продаже

---

### 3️⃣ Продажа и доставка

**Покупатель заказывает:**
- Выбирает дизайн открытки
- Оплачивает (490₽)
- Указывает адрес доставки

**Отправка:**
- Берем одну открытку из склада (статус `new`)
- Упаковываем с инструкцией:
  ```
  1. Отсканируйте QR или приложите телефон к открытке
  2. Создайте ваше видео-поздравление
  3. Подарите получателю
  ```
- Отправляем покупателю

---

### 4️⃣ Первое сканирование (Покупатель = Отправитель)

**Покупатель получает открытку:**

1. **Сканирует NFC или QR** → открывается `https://videomig.ru/c/A1B2C3D4`

2. **Backend обрабатывает:**
   ```javascript
   GET /api/cards/A1B2C3D4
   
   // Карточка найдена, статус: "new"
   // Автоматически меняем на "active"
   {
     id: "A1B2C3D4",
     status: "active", // ✅ Активирована!
     activatedAt: "2026-02-14T12:00:00Z"
   }
   ```

3. **Frontend показывает:** Страницу создания поздравления
   ```
   📹 Создайте ваше поздравление
   
   [Загрузить видео]
   [Добавить фото]
   [Написать текст]
   
   [Сохранить поздравление]
   ```

4. **Покупатель создает контент:**
   - Записывает видео (например, на телефон)
   - Загружает через форму
   - Добавляет текст: "С днем рождения, любимая!"
   - Нажимает "Сохранить"

5. **Backend сохраняет:**
   ```javascript
   POST /api/cards/A1B2C3D4/upload
   
   // Статус меняется на "filled"
   {
     id: "A1B2C3D4",
     status: "filled", // ✅ Заполнена!
     video: "/uploads/A1B2C3D4/video.mp4",
     photos: [...],
     message: "С днем рождения, любимая!",
     senderName: "Твой Саша",
     filledAt: "2026-02-14T12:05:00Z"
   }
   ```

6. **Frontend показывает:** "Готово! Теперь можно дарить ❤️"

---

### 5️⃣ Дарение получателю

**Покупатель дарит открытку:**
- Упаковывает красиво
- Дарит получателю
- Инструктирует: "Приложи телефон к открытке!"

---

### 6️⃣ Второе сканирование (Получатель)

**Получатель получает открытку:**

1. **Сканирует NFC или QR** → открывается `https://videomig.ru/c/A1B2C3D4`

2. **Backend обрабатывает:**
   ```javascript
   GET /api/cards/A1B2C3D4
   
   // Карточка найдена, статус: "filled"
   // Ничего не меняем, просто возвращаем данные
   {
     id: "A1B2C3D4",
     status: "filled",
     video: "/uploads/A1B2C3D4/video.mp4",
     message: "С днем рождения, любимая!",
     senderName: "Твой Саша"
   }
   ```

3. **Frontend показывает:** Страницу просмотра
   ```
   💝 Твой Саша отправил тебе поздравление
   
   [Видео играет автоматически]
   
   📝 "С днем рождения, любимая!"
   
   [Фотографии]
   ```

4. **Получатель наслаждается:**
   - Смотрит видео
   - Читает текст
   - Пересматривает снова и снова
   - Делится в соцсетях (виральность!)

---

### 7️⃣ Повторные просмотры

**Каждый раз при сканировании:**
- Статус остается `filled`
- Показывается страница просмотра
- Контент нельзя изменить (защита от случайного перезаписывания)

**Получатель может:**
- Пересматривать бесконечно
- Показывать друзьям и родным
- Хранить открытку годами

---

## Статусы карточки

| Статус | Описание | Кто видит | Что происходит |
|--------|----------|-----------|----------------|
| `new` | Новая, не отсканирована | Никто | Карточка на складе |
| `active` | Активирована, контент не загружен | Покупатель (отправитель) | Показывается форма создания |
| `filled` | Заполнена контентом | Получатель | Показывается просмотр |

---

## Важные моменты

### ✅ Защита от ошибок:

1. **Нельзя заполнить неактивированную карточку:**
   ```
   Статус "new" → Попытка загрузить контент
   Ответ: "Карточка еще не активирована. Отсканируйте QR/NFC сначала."
   ```

2. **Нельзя перезаписать заполненную карточку:**
   ```
   Статус "filled" → Попытка загрузить контент
   Ответ: "Карточка уже заполнена. Контент нельзя изменить."
   ```

3. **Автоматическая активация:**
   ```
   Статус "new" → GET /api/cards/{id}
   Автоматически меняется на "active"
   ```

### 🔄 Флоу-диаграмма:

```
[Генерация] → [new]
     ↓
[Печать NFC]
     ↓
[Продажа]
     ↓
[1-е сканирование] → [active]
     ↓
[Создание контента]
     ↓
[Сохранение] → [filled]
     ↓
[Дарение]
     ↓
[2-е сканирование] → [Просмотр]
     ↓
[∞ просмотры] → [Просмотр]
```

---

## Пример использования админ-панели

### Сценарий: Новогодняя партия

**Шаг 1: Генерация**
```
Админ → http://localhost:3001/admin
Количество: 200
Название: "Новый год 2026"
Жмем "Сгенерировать"
```

**Результат:**
```
✅ Создано 200 карточек
Партия ID: 1705228800000
Ссылки:
- A1B2C3D4 → https://videomig.ru/c/A1B2C3D4
- E5F6G7H8 → https://videomig.ru/c/E5F6G7H8
...
```

**Шаг 2: Экспорт**
```
Жмем "Скачать CSV"
Получаем файл: cards-export.csv
```

**Шаг 3: Производство**
```
Открываем CSV в Excel
Программируем NFC-метки по списку
Клеим на открытки
Упаковываем
```

**Шаг 4: Статистика**
```
Админ → "Обновить статистику"

Показывает:
- Всего: 200
- Новых: 150 (еще не проданы)
- Активных: 30 (куплены, но не заполнены)
- Заполненных: 20 (готовые поздравления)
```

---

## API Endpoints (обновленные)

### Для админа:

**Генерация карточек:**
```
POST /api/admin/generate-cards
Body: { count: 100, batchName: "Партия 1" }
Response: { success: true, batchId, cards: [...] }
```

**Экспорт CSV:**
```
GET /api/admin/export-cards?batchId=123
Response: CSV file download
```

### Для пользователей:

**Получение карточки (с авто-активацией):**
```
GET /api/cards/{cardId}
Response: { success: true, card: { status: "active"|"filled", ... } }
```

**Загрузка контента:**
```
POST /api/cards/{cardId}/upload
Body: FormData (video, photos, message, senderName)
Response: { success: true, card: { status: "filled", ... } }
```

**Статистика:**
```
GET /api/stats
Response: { success: true, stats: { total, new, active, filled } }
```

---

## Безопасность и валидация

### ✅ Проверки в backend:

1. **При получении карточки:**
   - Карточка существует?
   - Автоматическая активация если статус `new`

2. **При загрузке контента:**
   - Карточка существует?
   - Карточка не в статусе `new`? (должна быть активирована)
   - Карточка не в статусе `filled`? (нельзя перезаписать)
   - Видео загружено?
   - Размер файла в пределах лимита?

3. **При генерации:**
   - Количество не превышает 1000?
   - Уникальность ID гарантирована?

---

## Отличия от старой версии

### ❌ Было (неправильно):
- Карточки создавались по требованию (on-demand)
- При клике "Попробовать демо" → создавалась новая
- Статусы: `empty` / `filled`

### ✅ Стало (правильно):
- Карточки генерируются заранее (pre-generated)
- Админ создает партию → программирует NFC → продает
- Статусы: `new` / `active` / `filled`
- Первое сканирование активирует карточку

---

## Что дальше?

### Для production:
1. Добавить аутентификацию для админ-панели
2. Интегрировать с системой заказов
3. Добавить трекинг: кто, когда активировал/заполнил
4. Email уведомления при активации/заполнении
5. Аналитика по партиям

### Для масштабирования:
1. PostgreSQL вместо JSON файлов
2. S3 для видео/фото
3. CDN для быстрой загрузки
4. Кэширование часто используемых карточек

---

**Теперь флоу соответствует реальному производственному процессу! 🚀**
