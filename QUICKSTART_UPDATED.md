# ⚡ Быстрый старт ВидеоМиг MVP (Обновлено)

## 🎯 Что изменилось?

### ✅ Правильный флоу теперь реализован:

**Старая версия (неправильно):**
- Карточки создавались по клику "Попробовать демо"
- Статусы: `empty` / `filled`

**Новая версия (правильно):**
- Карточки генерируются заранее через админ-панель
- Статусы: `new` / `active` / `filled`
- Первое сканирование активирует карточку
- Админ может экспортировать CSV для NFC

---

## 🚀 Запуск за 3 минуты

### 1. Запустите проект

```bash
cd videocards-mvp

# Автоматический запуск (Linux/Mac)
./start.sh

# Или вручную:
# Терминал 1: cd backend && npm install && npm start
# Терминал 2: cd frontend && npm install && npm start
```

### 2. Откройте админ-панель

```
http://localhost:3001/admin
```

### 3. Сгенерируйте карточки

- Количество: 10 (для теста)
- Название партии: "Тестовая партия"
- Нажмите "Сгенерировать карточки"

**Результат:** Вы получите 10 уникальных ссылок

### 4. Протестируйте флоу

**Скопируйте любую ссылку, например:**
```
http://localhost:3000/c/A1B2C3D4
```

**Откройте в браузере:**
1. Первый раз → увидите форму создания (статус: `new` → `active`)
2. Загрузите видео и текст
3. Сохраните → статус станет `filled`
4. Обновите страницу → увидите просмотр

---

## 📋 Полный флоу работы

### Производственный сценарий:

**Шаг 1: Админ генерирует партию**
```
Админ-панель → Генерация → 100 карточек
↓
Получает CSV файл со ссылками
```

**Шаг 2: Производство**
```
CSV → Программирование NFC-меток
NFC-метки → Клеятся на открытки
QR-коды → Печатаются на обороте
↓
100 готовых открыток
```

**Шаг 3: Продажа**
```
Покупатель заказывает → Оплачивает 490₽
↓
Получает открытку по почте (статус: "new")
```

**Шаг 4: Покупатель создает поздравление**
```
Сканирует NFC/QR (1-й раз)
↓
Статус: "new" → "active" (автоматически)
↓
Видит форму создания
↓
Загружает видео, фото, текст
↓
Сохраняет → статус: "filled"
```

**Шаг 5: Получатель смотрит**
```
Получает открытку в подарок
↓
Сканирует NFC/QR (2-й раз)
↓
Видит готовое поздравление
↓
Может пересматривать ∞ раз
```

---

## 🎨 Админ-панель

### Доступ:
```
http://localhost:3001/admin
```

### Функции:

**1. Генерация карточек**
- Создает партию с уникальными ID
- Каждая получает статус "new"
- Готовы к программированию NFC

**2. Экспорт CSV**
- Формат: `ID, URL, Status, Batch, Created`
- Используется для программирования NFC-меток
- Можно фильтровать по партии

**3. Статистика**
- **Всего:** общее количество карточек
- **Новых:** еще не отсканированы (на складе)
- **Активных:** отсканированы, но контент не загружен
- **Заполненных:** готовые поздравления

---

## 📊 Статусы карточек

| Статус | Когда | Что видит пользователь |
|--------|-------|------------------------|
| `new` | После генерации | Карточка на складе, еще не продана |
| `active` | После 1-го сканирования | Форма создания поздравления |
| `filled` | После сохранения контента | Страница просмотра с видео |

---

## 🔧 API для разработчиков

### Admin endpoints:

```javascript
// Генерация партии
POST /api/admin/generate-cards
Body: { count: 100, batchName: "Партия 1" }
Response: { success: true, batchId, cards: [...] }

// Экспорт CSV
GET /api/admin/export-cards?batchId=123
Response: CSV file download
```

### Public endpoints:

```javascript
// Получение карточки (с авто-активацией)
GET /api/cards/:cardId
Response: { success: true, card: { status, ... } }

// Загрузка контента
POST /api/cards/:cardId/upload
Body: FormData (video, photos, message, senderName)
Response: { success: true, card: { status: "filled", ... } }

// Статистика
GET /api/stats
Response: { success: true, stats: { total, new, active, filled } }
```

---

## 🎯 Пример тестирования

### Сценарий 1: Генерация и активация

```bash
# 1. Откройте админ-панель
http://localhost:3001/admin

# 2. Создайте 5 карточек
Количество: 5
Партия: "Тест"
→ Жмите "Сгенерировать"

# 3. Скопируйте любую ссылку
Например: http://localhost:3000/c/ABC123

# 4. Откройте ссылку
→ Увидите форму создания
→ Статус автоматически изменится на "active"

# 5. Проверьте статистику
Обновите админ-панель
→ Новых: 4, Активных: 1, Заполненных: 0
```

### Сценарий 2: Создание поздравления

```bash
# 1. На странице создания:
- Загрузите тестовое видео
- Добавьте текст: "С днем рождения!"
- Имя: "Тестовый отправитель"

# 2. Сохраните
→ Статус изменится на "filled"

# 3. Обновите страницу
→ Теперь видите просмотр (а не форму)

# 4. Проверьте статистику
→ Новых: 4, Активных: 0, Заполненных: 1
```

---

## 🔒 Защита от ошибок

### Система не позволит:

**1. Загрузить контент на неактивированную карточку:**
```
Статус: "new"
Попытка: POST /api/cards/ABC/upload
Ответ: 400 "Карточка еще не активирована"
```

**2. Перезаписать заполненную карточку:**
```
Статус: "filled"
Попытка: POST /api/cards/ABC/upload
Ответ: 400 "Карточка уже заполнена. Контент нельзя изменить."
```

**3. Сгенерировать слишком много карточек:**
```
Попытка: 1001 карточка
Ответ: 400 "Максимум 1000 карточек за раз"
```

---

## 📱 Реальный пример использования

### Типография "Подарки":

**Месяц 1:**
```
1. Генерируем 500 карточек → партия "Январь 2026"
2. Программируем NFC-метки
3. Печатаем открытки
4. Размещаем в магазине
```

**Продажи:**
```
- Продано: 150 открыток
- Активировано: 120 (80% покупателей создали контент)
- Заполнено: 115 (96% активировавших завершили создание)
```

**Статистика в админке:**
```
Всего: 500
Новых: 350 (на складе)
Активных: 5 (купили, но еще не заполнили)
Заполненных: 115 (готовые поздравления)
```

---

## 🎁 Что дальше?

### Для локального теста:
- ✅ Попробуйте создать несколько карточек
- ✅ Протестируйте все статусы
- ✅ Проверьте защиту от ошибок

### Для production:
- 📖 Читайте [DEPLOYMENT.md](DEPLOYMENT.md)
- ☁️ Настройте хостинг
- 🗄️ Мигрируйте на PostgreSQL
- 📦 Настройте S3 для файлов

### Для бизнеса:
- 📱 Создайте соцсети (см. [MARKETING.md](MARKETING.md))
- 🎬 Подготовьте контент
- 🚀 Запустите pre-launch

---

## 📚 Дополнительная документация

- **[README.md](README.md)** — полное описание проекта
- **[WORKFLOW.md](WORKFLOW.md)** — детальный флоу работы
- **[BRANDBOOK.md](BRANDBOOK.md)** — брендбук
- **[MARKETING.md](MARKETING.md)** — маркетинг план
- **[DEPLOYMENT.md](DEPLOYMENT.md)** — production деплой

---

**Теперь флоу полностью соответствует реальному производству! 🚀**
